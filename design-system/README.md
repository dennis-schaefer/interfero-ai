# Interfero Design System

This project contains the design system used for Interfero. It's build with React, TypeScript, and shadcn/ui.
https://ui.shadcn.com/create?preset=b1FSSCP0y&base=base

## Starting the app
To start the app with the design system locally, run the following command:

```bash
npm run dev
```